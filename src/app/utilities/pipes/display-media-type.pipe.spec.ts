import { DisplayMediaTypePipe } from './display-media-type.pipe';

describe('DisplayMediaTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayMediaTypePipe();
    expect(pipe).toBeTruthy();
  });
});
